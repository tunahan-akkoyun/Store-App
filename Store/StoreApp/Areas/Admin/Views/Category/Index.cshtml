@model IEnumerable<Category>

<div class="text-center my-5 border-bottom">
    <div class="display-6">Categoriler</div>
</div>

<ul class="list-group my-3">
    @foreach (var cat in Model)
    {
        <li class="list-group-item">@cat.CategoryName</li>
    }
</ul>